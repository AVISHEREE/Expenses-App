// Use VITE_API_BASE_URL environment variable for production (Vercel)
// In development, falls back to localhost:3000
// Set this in Vercel dashboard: VITE_API_BASE_URL = https://your-backend.com
export const BASE_URL = 'https://expenses-app-rxsa.onrender.com';

// Legacy export kept for compatibility (no longer used directly)
export const lH = 'expenses-app-rxsa.onrender.com';